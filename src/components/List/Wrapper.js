import styled from 'styled-components';

const Wrapper = styled.div`
    .ms-DetailsRow-cell {
        padding: 0;
    }

    .ms-DetailsRow-cell > * {
        display: block;
        padding: 11px 8px;
    }
`;

export default Wrapper;